# m2.toml - m2bytes: Byte buffer + binary codec library
#
# Growable byte buffer, cursor reader/writer, varint codec, hex helpers.
# No heap allocation in hot paths beyond buffer growth.
# Core depends only on SYSTEM, Storage, InOut, and Strings.
manifest_version=1
name=m2bytes
version=1.2.0
edition=pim4
entry=src/ByteBuf.mod
includes=src

[deps]

[test]
entry=tests/bytes_tests.mod

[cc]
